apiVersion: v1
kind: Service
metadata:
  name: local-confluent-kafka-cp-kafka-rest
  labels:
    app: cp-kafka-rest
    chart: cp-kafka-rest-0.1.0
    release: local-confluent-kafka
    heritage: Helm
spec:
  ports:
    - name: rest-proxy
      port: 8082
    - name: metrics
      port: 5556
  selector:
    app: cp-kafka-rest
    release: local-confluent-kafka